import React from "react"

const UserContext = React.createContext();  
// Here No need to write React as we can see we import React already if we not done then only we have to do

export default UserContext;